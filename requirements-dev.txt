# Development dependencies (tests, formatting, linting, notebooks).
# Install on top of requirements.txt:
#
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt

pytest==9.0.2
black==25.9.0
flake8==7.3.0
jupyter==1.1.1
