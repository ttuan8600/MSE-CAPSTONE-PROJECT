# REST API dependencies for app.py.
# Install on top of requirements.txt:
#
#   pip install -r requirements.txt
#   pip install -r requirements_api.txt
#
# This file intentionally does NOT pin torch/numpy/scipy/scikit-learn. Those come
# from requirements.txt; pinning them again here previously produced a conflicting
# resolution (torch 2.0.0 vs 2.7.1, numpy 1.24.3 vs 2.4.2).

flask==3.1.2
flask-cors==6.0.1
requests==2.32.5

# Production WSGI server. Not available on Windows.
gunicorn==23.0.0; sys_platform != "win32"
